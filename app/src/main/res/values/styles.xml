<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <!--    Ripple-->
    <style name="RippleViewTheme">
        <item name="android:colorControlHighlight">@color/colorRipple</item>
    </style>
    <style name="RippleViewBackground">
        <item name="android:theme">@style/RippleViewTheme</item>
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>
    </style>

    <style name="tallerBarStyle" parent="Widget.AppCompat.SeekBar">
<!--        <item name="android:indeterminateOnly">false</item>-->
<!--        <item name="android:progressDrawable">@android:drawable/progress_horizontal</item>-->
<!--        <item name="android:indeterminateDrawable">@android:drawable/progress_horizontal</item>-->
        <item name="android:maxHeight">6dip</item>
    </style>

</resources>
